Solutions to advent of code puzzles using elixir.

## How to run?

* Put `input.txt` and `sample.txt` near `solution.exs` file
* Run solution with `elixir solution.exs`

Solutions have worked using Elixir 1.19.4 (compiled with Erlang/OTP 28).